sudo: required
dist: trusty
language: cpp
compiler: g++
install: export CXX="g++-5"
addons:
  apt:
    packages:
    - g++-5
notifications:
 email:
  on_success: never
  on_failure: always
before_install: sudo apt update -qq && sudo apt -y install libboost-all-dev
script: 
  - mkdir build && cd build
  - cnake ..
  - make